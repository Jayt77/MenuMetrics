# Configuration

Use these files as templates; copy to project root or set env vars.
- sample.env: example environment variables for local runs.
- .streamlit/config.toml: Streamlit UI theme and server defaults (already present).
- Adjust paths in DATA_DIR if moving data folder.
